repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.13.2
    hooks:
      # 린팅 (확인만, make format의 첫 번째 명령어와 동일)
      - id: ruff
        files: ^src/
      # 포맷팅 (확인만, make format의 두 번째 명령어와 동일)  
      - id: ruff-format
        args: [--check]
        files: ^src/
